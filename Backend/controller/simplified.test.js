// A simplified test file to ensure Jest is working properly
describe('Simplified Test Suite', () => {
  test('should pass this simple test', () => {
    expect(true).toBe(true);
  });

  test('should handle basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
